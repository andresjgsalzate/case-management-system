"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.originRoutes = void 0;
const express_1 = require("express");
const OriginController_1 = require("../controllers/OriginController");
const auth_1 = require("../middleware/auth");
const auditMiddleware_1 = require("../middleware/auditMiddleware");
const router = (0, express_1.Router)();
exports.originRoutes = router;
const originController = new OriginController_1.OriginController();
router.use(auth_1.authenticateToken);
router.use(auditMiddleware_1.AuditMiddleware.initializeAuditContext);
router.get("/", originController.getAllOrigins.bind(originController));
router.get("/search", originController.searchOrigins.bind(originController));
router.get("/stats", originController.getOriginStats.bind(originController));
router.get("/:id", originController.getOriginById.bind(originController));
router.post("/", auditMiddleware_1.AuditMiddleware.auditCreate("origenes"), originController.createOrigin.bind(originController));
router.put("/:id", auditMiddleware_1.AuditMiddleware.auditUpdate("origenes"), originController.updateOrigin.bind(originController));
router.delete("/:id", auditMiddleware_1.AuditMiddleware.auditDelete("origenes"), originController.deleteOrigin.bind(originController));
router.get("/:id/can-delete", originController.checkCanDeleteOrigin.bind(originController));
